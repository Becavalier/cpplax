fn fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2); 
}

var before = clock();
print(fib(30));
var after = clock();
print("\n");
print(before);
print("\n");
print(after);
print("\n");
print(after - before);
